using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ClearGO : MonoBehaviour
{

    // Start is called before the first frame update
    void Start()
    {
        MusicPlayer.ist.StartMusicList(6);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
